diff --git a/ggml/include/ggml-backend.h b/ggml/include/ggml-backend.h
index 2924fdbe..d5fcaaa3 100644
--- a/ggml/include/ggml-backend.h
+++ b/ggml/include/ggml-backend.h
@@ -313,6 +313,27 @@ extern "C" {
     //
     typedef bool (*ggml_backend_sched_eval_callback)(struct ggml_tensor * t, bool ask, void * user_data);
 
+    // Replacement callback for nodes that an external accelerator computes.
+    // When ask is true, return true to exclude the node from backend execution.
+    // When ask is false, compute the node and return false to abort the graph.
+    typedef bool (*ggml_backend_sched_eval_override_callback)(
+        struct ggml_tensor * t,
+        bool ask,
+        void * user_data);
+
+    enum ggml_backend_sched_eval_profile_phase {
+        GGML_BACKEND_SCHED_EVAL_PROFILE_QUERY,
+        GGML_BACKEND_SCHED_EVAL_PROFILE_BEGIN,
+        GGML_BACKEND_SCHED_EVAL_PROFILE_END,
+    };
+
+    // Profile selected nodes without replacing them. Return true for QUERY to isolate a node.
+    // BEGIN and END bracket only that node's synchronized backend execution.
+    typedef bool (*ggml_backend_sched_eval_profile_callback)(
+        struct ggml_tensor * t,
+        enum ggml_backend_sched_eval_profile_phase phase,
+        void * user_data);
+
     // Initialize a backend scheduler, backends with low index are given priority over backends with high index
     GGML_API ggml_backend_sched_t ggml_backend_sched_new(ggml_backend_t * backends, ggml_backend_buffer_type_t * bufts, int n_backends, size_t graph_size, bool parallel, bool op_offload);
     GGML_API void                 ggml_backend_sched_free(ggml_backend_sched_t sched);
@@ -351,6 +372,17 @@ extern "C" {
     // Set a callback to be called for each resulting node during graph compute
     GGML_API void                 ggml_backend_sched_set_eval_callback(ggml_backend_sched_t sched, ggml_backend_sched_eval_callback callback, void * user_data);
 
+    // Set a callback that replaces selected nodes during graph compute.
+    GGML_API void ggml_backend_sched_set_eval_override_callback(
+        ggml_backend_sched_t sched,
+        ggml_backend_sched_eval_override_callback callback,
+        void * user_data);
+
+    GGML_API void ggml_backend_sched_set_eval_profile_callback(
+        ggml_backend_sched_t sched,
+        ggml_backend_sched_eval_profile_callback callback,
+        void * user_data);
+
     //
     // Meta backend
     //
diff --git a/ggml/src/ggml-backend.cpp b/ggml/src/ggml-backend.cpp
index 7f4e252d..0d092755 100644
--- a/ggml/src/ggml-backend.cpp
+++ b/ggml/src/ggml-backend.cpp
@@ -813,6 +813,12 @@ struct ggml_backend_sched {
     ggml_backend_sched_eval_callback callback_eval;
     void * callback_eval_user_data;
 
+    ggml_backend_sched_eval_override_callback callback_eval_override;
+    void * callback_eval_override_user_data;
+
+    ggml_backend_sched_eval_profile_callback callback_eval_profile;
+    void * callback_eval_profile_user_data;
+
     char * context_buffer;
     size_t context_buffer_size;
 
@@ -1683,7 +1689,83 @@ static enum ggml_status ggml_backend_sched_compute_splits(ggml_backend_sched_t s
             }
         }
 
-        if (!sched->callback_eval) {
+        if (sched->callback_eval_override) {
+            for (int j0 = 0; j0 < split->graph.n_nodes;) {
+                int j1 = j0;
+                while (j1 < split->graph.n_nodes &&
+                       !sched->callback_eval_override(
+                           split->graph.nodes[j1],
+                           true,
+                           sched->callback_eval_override_user_data)) {
+                    j1++;
+                }
+
+                if (j1 > j0) {
+                    struct ggml_cgraph gv = ggml_graph_view(&split->graph, j0, j1);
+                    enum ggml_status ec = ggml_backend_graph_compute_async(
+                        split_backend,
+                        &gv);
+                    if (ec != GGML_STATUS_SUCCESS) {
+                        return ec;
+                    }
+                    ggml_backend_synchronize(split_backend);
+                }
+
+                if (j1 == split->graph.n_nodes) {
+                    break;
+                }
+
+                if (!sched->callback_eval_override(
+                        split->graph.nodes[j1],
+                        false,
+                        sched->callback_eval_override_user_data)) {
+                    return GGML_STATUS_ABORTED;
+                }
+                j0 = j1 + 1;
+            }
+        } else if (sched->callback_eval_profile) {
+            for (int j0 = 0; j0 < split->graph.n_nodes;) {
+                int j1 = j0;
+                while (j1 < split->graph.n_nodes &&
+                       !sched->callback_eval_profile(
+                           split->graph.nodes[j1],
+                           GGML_BACKEND_SCHED_EVAL_PROFILE_QUERY,
+                           sched->callback_eval_profile_user_data)) {
+                    j1++;
+                }
+
+                if (j1 > j0) {
+                    struct ggml_cgraph gv = ggml_graph_view(&split->graph, j0, j1);
+                    enum ggml_status ec = ggml_backend_graph_compute_async(
+                        split_backend,
+                        &gv);
+                    if (ec != GGML_STATUS_SUCCESS) {
+                        return ec;
+                    }
+                    ggml_backend_synchronize(split_backend);
+                }
+
+                if (j1 == split->graph.n_nodes) {
+                    break;
+                }
+
+                sched->callback_eval_profile(
+                    split->graph.nodes[j1],
+                    GGML_BACKEND_SCHED_EVAL_PROFILE_BEGIN,
+                    sched->callback_eval_profile_user_data);
+                struct ggml_cgraph gv = ggml_graph_view(&split->graph, j1, j1 + 1);
+                enum ggml_status ec = ggml_backend_graph_compute_async(split_backend, &gv);
+                if (ec != GGML_STATUS_SUCCESS) {
+                    return ec;
+                }
+                ggml_backend_synchronize(split_backend);
+                sched->callback_eval_profile(
+                    split->graph.nodes[j1],
+                    GGML_BACKEND_SCHED_EVAL_PROFILE_END,
+                    sched->callback_eval_profile_user_data);
+                j0 = j1 + 1;
+            }
+        } else if (!sched->callback_eval) {
             enum ggml_status ec = ggml_backend_graph_compute_async(split_backend, &split->graph);
             if (ec != GGML_STATUS_SUCCESS) {
                 return ec;
@@ -1929,6 +2011,24 @@ void ggml_backend_sched_set_eval_callback(ggml_backend_sched_t sched, ggml_backe
     sched->callback_eval_user_data = user_data;
 }
 
+void ggml_backend_sched_set_eval_override_callback(
+        ggml_backend_sched_t sched,
+        ggml_backend_sched_eval_override_callback callback,
+        void * user_data) {
+    GGML_ASSERT(sched);
+    sched->callback_eval_override = callback;
+    sched->callback_eval_override_user_data = user_data;
+}
+
+void ggml_backend_sched_set_eval_profile_callback(
+        ggml_backend_sched_t sched,
+        ggml_backend_sched_eval_profile_callback callback,
+        void * user_data) {
+    GGML_ASSERT(sched);
+    sched->callback_eval_profile = callback;
+    sched->callback_eval_profile_user_data = user_data;
+}
+
 int ggml_backend_sched_get_n_splits(ggml_backend_sched_t sched) {
     GGML_ASSERT(sched);
     return sched->n_splits;
diff --git a/include/llama.h b/include/llama.h
index 3c6d22be..30467978 100644
--- a/include/llama.h
+++ b/include/llama.h
@@ -375,6 +375,12 @@ extern "C" {
         ggml_backend_sched_eval_callback cb_eval;
         void * cb_eval_user_data;
 
+        ggml_backend_sched_eval_override_callback cb_eval_override;
+        void * cb_eval_override_user_data;
+
+        ggml_backend_sched_eval_profile_callback cb_eval_profile;
+        void * cb_eval_profile_user_data;
+
         enum ggml_type type_k; // data type for K cache [EXPERIMENTAL]
         enum ggml_type type_v; // data type for V cache [EXPERIMENTAL]
 
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index 9b399d60..e4de1d30 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -136,6 +136,10 @@ llama_context::llama_context(
 
     cparams.cb_eval           = params.cb_eval;
     cparams.cb_eval_user_data = params.cb_eval_user_data;
+    cparams.cb_eval_override = params.cb_eval_override;
+    cparams.cb_eval_override_user_data = params.cb_eval_override_user_data;
+    cparams.cb_eval_profile = params.cb_eval_profile;
+    cparams.cb_eval_profile_user_data = params.cb_eval_profile_user_data;
 
     cparams.ctx_other = nullptr;
 
@@ -1344,6 +1348,14 @@ llm_graph_result * llama_context::process_ubatch(const llama_ubatch & ubatch, ll
 
         ggml_backend_sched_reset(sched.get());
         ggml_backend_sched_set_eval_callback(sched.get(), cparams.cb_eval, cparams.cb_eval_user_data);
+        ggml_backend_sched_set_eval_override_callback(
+            sched.get(),
+            cparams.cb_eval_override,
+            cparams.cb_eval_override_user_data);
+        ggml_backend_sched_set_eval_profile_callback(
+            sched.get(),
+            cparams.cb_eval_profile,
+            cparams.cb_eval_profile_user_data);
 
         //const auto t_start_us = ggml_time_us();
 
@@ -3489,6 +3501,10 @@ llama_context_params llama_context_default_params() {
         /*.defrag_thold                =*/ -1.0f,
         /*.cb_eval                     =*/ nullptr,
         /*.cb_eval_user_data           =*/ nullptr,
+        /*.cb_eval_override            =*/ nullptr,
+        /*.cb_eval_override_user_data  =*/ nullptr,
+        /*.cb_eval_profile             =*/ nullptr,
+        /*.cb_eval_profile_user_data   =*/ nullptr,
         /*.type_k                      =*/ GGML_TYPE_F16,
         /*.type_v                      =*/ GGML_TYPE_F16,
         /*.abort_callback              =*/ nullptr,
diff --git a/src/llama-cparams.h b/src/llama-cparams.h
index 5018170e..373f7a6b 100644
--- a/src/llama-cparams.h
+++ b/src/llama-cparams.h
@@ -61,5 +61,11 @@ struct llama_cparams {
     ggml_backend_sched_eval_callback cb_eval;
     void * cb_eval_user_data;
 
+    ggml_backend_sched_eval_override_callback cb_eval_override;
+    void * cb_eval_override_user_data;
+
+    ggml_backend_sched_eval_profile_callback cb_eval_profile;
+    void * cb_eval_profile_user_data;
+
     llama_context * ctx_other;
 };
