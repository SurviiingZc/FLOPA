# FPGA Board-Level Test Documents

- `attention_test_plan.md`: Re10K and LLM Attention board test plan.

The RTL sources remain under `rtl/` and the Vivado/Vitis integration entry points
remain under `fpga/`. This directory contains board-level test methodology,
workload manifests, sampling rules, and acceptance criteria only.
